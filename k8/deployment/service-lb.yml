---
apiVersion: v1
kind: Service
metadata:
  name: loadbalancer
  namespace: development
  labels:
    app: nginx-deployment
spec:
  selector:
    app: nginx-deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080
  type: NodePort