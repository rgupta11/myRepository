package com.rga.q.crackingCodingQ.stack;


/**
 * Create a HashMap lookup with closing braces as keys and opening braces as values
 * for any expresssion check the braces one by one
 * if opening brace push to stack
 * if closing brace pop from stack and check if it matches the value from map 
 * for the closing brace.
 * if not matching or stack is empty then expression is invalid
 * at end of expression if stack is not empty then expression is invalid
 */
public class ParenthesisCheck {
    
    
    
}
